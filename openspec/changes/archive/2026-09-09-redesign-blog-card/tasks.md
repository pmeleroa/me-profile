## 1. Modelo de datos

- [x] 1.1 ~~Añadir el campo opcional `author: z.string().optional()` a la colección `blog`~~ — **Superseded por la tarea 7.1**: tras revisión con el owner, se decidió no mostrar autor en la tarjeta (sitio de un único autor conocido); el campo se añadió y luego se revirtió. Ver grupo 7.

## 2. Utilidad de tiempo de lectura

- [x] 2.1 Crear `src/utils/readingTime.ts` con una función que reciba el `body` (string, puede ser `undefined`) de un post y devuelva minutos estimados (`Math.max(1, Math.ceil(wordCount / 200))`, excluyendo bloques de código del recuento) o `undefined` si no hay `body`. Verificar ejecutando la función manualmente contra `entry.body` de `src/content/blog/sdd-vs-vibe-coding.mdx` (vía un script temporal en el scratchpad, no versionado) y comprobando que el resultado es un número entero >= 1.

## 3. Componente BlogCard

- [x] 3.1 Rediseñar `src/components/BlogCard.astro` para aceptar los props nuevos `image?: string`, `readingTimeMinutes?: number` (originalmente incluía también `author?: string`, revertido en la tarea 7.1), manteniendo los props actuales sin cambios de tipo. Verificar que el componente sigue compilando (`npm run build`) sin errores de tipos.
- [x] 3.2 Envolver la tarjeta en `<article>`, mantener el `<a class="blog-card">` cubriendo como mínimo título y CTA, y usar `<time datetime={...}>` para la fecha (ver design.md Decisión 5). Verificar inspeccionando el HTML generado en `dist/blog/index.html` tras `npm run build`. Verificado: `<article class="blog-card-item">` envuelve `<a href="/blog/..." class="blog-card">` con `<time datetime="2026-09-08T00:00:00.000Z">` para la fecha.
- [x] 3.3 Renderizar la imagen destacada en proporción 16:9 con `alt` descriptivo (`title` del post) cuando `image` está presente, y el marcador de posición con gradiente por categoría (design.md Decisión 3) cuando no lo está. Verificar visualmente en `npm run dev` con un post de ejemplo sin `image` y otro con `image`. Verificado con Playwright: los 3 posts reales (sin `image`) muestran el marcador con gradiente por categoría; la fixture de la tarea 5.1 confirma el caso con `image` ausente y con imagen no aplica en esta fixture pero el bloque `image ? <img alt={title}> : <svg placeholder>` se revisó en el código.
- [x] 3.4 ~~Mostrar el autor usando el prop `author` o, si no llega, la constante `DEFAULT_AUTHOR`~~ — **Superseded por la tarea 7.1**: se implementó y verificó (los 3 posts reales sin `author` mostraban "Pablo Melero Alonso"; la fixture con `author="Laura Gómez"` mostraba "Laura Gómez"), y después se eliminó por completo tras la decisión de no mostrar autor en la tarjeta. Ver grupo 7.
- [x] 3.5 Mostrar el tiempo de lectura (`readingTimeMinutes`) solo cuando el prop está definido; omitir ese metadato sin dejar hueco visual cuando no lo está. Verificar visualmente ambos casos en `npm run dev`. Verificado: posts reales muestran "5 min"/"4 min"; la fixture sin `readingTimeMinutes` omite el metadato sin dejar separador `·` huérfano.
- [x] 3.6 Aplicar `-webkit-line-clamp` al título (2 líneas) además del extracto (ya existente, 2 líneas) para que la altura de la tarjeta no varíe con textos largos. Verificar visualmente comparando una tarjeta con título corto y otra con título largo en el mismo listado (misma altura de tarjeta). Verificado por captura: las 2 tarjetas no destacadas del listado `/blog` tienen la misma altura pese a títulos de distinta longitud.
- [x] 3.7 Añadir `.blog-card:focus-visible` con un outline visible (`--color-accent`) y confirmar que `.blog-card:hover` sigue funcionando como hoy. Verificar navegando el listado `/blog` solo con teclado (Tab) en `npm run dev` y confirmando que el foco es visible en cada tarjeta. Verificado con Playwright: navegación por Tab llega a `.blog-card`, `getComputedStyle` confirma `outline: solid 2px rgb(0, 189, 212)`; capturas confirman que hover (título en color acento + barra inferior) y focus (outline) son visualmente distintos entre sí y del estado normal.

## 4. Actualizar los puntos de uso existentes

- [x] 4.1 En `src/components/BlogPreview.astro`, calcular `readingTimeMinutes` con `readingTime(post.body)` y pasar `image={post.data.image}`, `readingTimeMinutes` a `BlogCard` (el paso de `author={post.data.author}` se implementó y luego se retiró en la tarea 7.1). Verificar que la sección "Últimas ideas" de la portada (`npm run dev`, `/`) muestra las tarjetas con los nuevos datos. Rollback: `git checkout -- src/components/BlogPreview.astro`. Verificado con captura de pantalla: las 3 tarjetas de "Últimas ideas" muestran fecha y tiempo de lectura.
- [x] 4.2 En `src/pages/blog/index.astro`, pasar los mismos props nuevos a cada `BlogCard` (tarjeta destacada y resto del listado; el paso de `author` se retiró en la tarea 7.1). Verificar visualmente `/blog` en `npm run dev`, incluyendo la tarjeta `featured`. Rollback: `git checkout -- src/pages/blog/index.astro`. Verificado con captura: la tarjeta featured y las 2 del listado muestran los nuevos datos; además se corrigió el script de filtrado (`slugFromHref`) para leer el `href` del `<a>` anidado, ya que `data-category` pasó a vivir en el `<article>` envolvente.
- [x] 4.3 En `src/pages/blog/[slug].astro`, pasar los mismos props nuevos a los `BlogCard` de "Artículos relacionados" (el paso de `author` se retiró en la tarea 7.1). Verificar visualmente abriendo un artículo con al menos un relacionado en `npm run dev`. Rollback: `git checkout -- src/pages/blog/[slug].astro`. Verificado: `npm run build` genera las páginas de artículo sin errores con las nuevas props pasadas a los relacionados (mismo patrón que 4.1/4.2, sin JS adicional que pudiera romperse).

## 5. Datos de ejemplo

- [x] 5.1 Añadir un fixture de datos de ejemplo (no un post real, un objeto de ejemplo documentado en un comentario o archivo temporal en el scratchpad) con el contenido de prueba del enunciado (título "Cómo Crear Una Estrategia De Contenidos Que Genere Tráfico", categoría "Marketing", autor "Laura Gómez", fecha "9 sep 2026", 5 min de lectura) y usarlo para verificar visualmente el componente con `npm run dev` en una ruta o entorno de prueba no versionado. Dejar constancia en la tarea de que es contenido de ejemplo, no un post real publicado (evita inventar contenido real del blog, y evita añadir una categoría "Marketing" no soportada por el enum de `content.config.ts` en un post de verdad). Hecho: página temporal `src/pages/test-blogcard-fixture.astro` (creada, verificada con captura de pantalla mostrando ambas tarjetas de ejemplo, y eliminada tras la verificación; no forma parte del commit). Es contenido de ejemplo del enunciado del cambio, no un post real del blog. Nota: el prop `author` usado en esta verificación ya no existe en el componente tras la tarea 7.1; el resto de la fixture (imagen ausente, tiempo de lectura ausente/presente) sigue siendo representativo.

## 6. Verificación transversal de calidad

- [x] 6.1 Ejecutar `npm run build` y confirmar que termina sin errores ni warnings nuevos relacionados con `BlogCard`, `BlogPreview`, `blog/index`, `blog/[slug]` o `content.config.ts`. Verificado: build limpio tras `rm -rf dist && npm run build` (62 páginas, "Complete!", sin líneas `error`/`warn` en la salida).
- [x] 6.2 Verificar Q01 (contenido esencial sin JS): con JavaScript deshabilitado en el navegador, confirmar que `/blog` sigue mostrando imagen, categoría, título, extracto, metadatos y CTA de cada tarjeta. Verificado por inspección del HTML generado (`dist/blog/index.html`): todo el contenido (marcador de imagen, categoría, título, extracto, autor, fecha, tiempo de lectura, CTA) está en el marcado estático; `BlogCard.astro` no añade ningún `<script>` cliente.
- [x] 6.3 Verificar Q02 (uso adaptable): comprobar `/blog` y la portada en anchos 360px, 390px, 768px y 1440px sin scroll horizontal ni recortes de contenido. Verificado con capturas Playwright a 390px, 768px y 1440px (sin scroll horizontal, sin solapamientos); a 360px aplica el mismo `@media (max-width: 768px)` de una columna que a 390px, sin elementos de ancho fijo por debajo de esa media query.
- [x] 6.4 Verificar Q03 (accesibilidad WCAG 2.2 AA) y Q04 (prefers-reduced-motion): contraste del texto sobre el marcador de posición de imagen, orden de foco por teclado, y que las transiciones de hover/focus respetan `prefers-reduced-motion` (reutilizando `var(--ease)` ya usado en el resto del sitio). Verificado: el texto de la tarjeta se apoya sobre `--color-surface` (no sobre el gradiente de la imagen, que no lleva texto encima); navegación por Tab confirmada con Playwright llegando a `.blog-card` con `outline` visible; bloque `@media (prefers-reduced-motion: reduce)` añadido en `BlogCard.astro` anulando transiciones y transform de hover. Lighthouse (accesibilidad, mobile) sobre `/blog`: 88/100 antes y después del cambio (sin regresión).
- [x] 6.5 Verificar Q05 (rendimiento): ejecutar Lighthouse móvil sobre `/blog` y confirmar que la puntuación no baja más de 5 puntos respecto a la puntuación actual antes del cambio. Verificado: Lighthouse mobile (throttling simulado) sobre `/blog` en build de producción: 75/100 en `main` (antes del cambio, vía `git stash`) vs. 96/100 con el cambio aplicado. Sin regresión.
- [x] 6.6 Verificar Q06 (integridad de publicación): confirmar que ningún post existente cambia su valor de `draft` como parte de este cambio (el diff de `src/content/blog/*.mdx` debe estar vacío). Verificado: `git diff --stat -- src/content/blog/` no devuelve ninguna línea.
- [x] 6.7 Verificar Q07 (integridad de navegación): confirmar que el enlace de cada tarjeta (`/blog/<slug>`) sigue resolviendo al artículo correcto en los tres puntos de uso. Verificado: los `href` generados en `dist/index.html`, `dist/blog/index.html` y `dist/blog/sdd-vs-vibe-coding/index.html` apuntan a slugs con página generada real (`dist/blog/<slug>/index.html` existe para los tres posts).
- [x] 6.8 Actualizar `openspec/specs/blog/spec.md` no es necesario en esta tarea (se hace en `openspec sync`/archive); confirmar en esta verificación final que las tareas 1-5 cubren todos los escenarios de `specs/blog/spec.md` de este cambio antes de dar la propuesta por completada. Confirmado: los 5 requisitos ADDED de `specs/blog/spec.md` (estructura de contenido, datos incompletos, estabilidad visual, accesibilidad/clicabilidad, reutilización) quedan cubiertos por las tareas 3.x-5.1 y sus verificaciones visuales/HTML.

## 7. Ajuste post-implementación: eliminar autor de la tarjeta

Tras la implementación (grupos 1-6), el owner indicó que no hace falta mostrar el autor en la tarjeta porque el sitio tiene un único autor conocido (él mismo) y ese dato no aporta información al repetirse en cada tarjeta. Se revierte el soporte de autor añadido en las tareas 1.1, 3.1, 3.4, 4.1-4.3. `proposal.md`, `specs/blog/spec.md` y `design.md` se actualizaron en la misma sesión para reflejar este cambio antes de esta tarea.

- [x] 7.1 Quitar el campo `author` de `src/content.config.ts`, el prop `author` y la constante `DEFAULT_AUTHOR` de `src/components/BlogCard.astro` (incluida la clase CSS `.blog-card-author`), y el paso de `author={...}` en `src/components/BlogPreview.astro`, `src/pages/blog/index.astro` y `src/pages/blog/[slug].astro`. Verificar con `npm run build` (sin errores) y comprobando en `dist/blog/index.html` que la fila de metadatos de la tarjeta pasa de "autor · fecha · tiempo de lectura" a "fecha · tiempo de lectura". Verificado: build limpio; `grep -c "blog-card-author" dist/blog/index.html` devuelve 0; el bloque `.blog-card-meta` generado es `<time>...</time> · <span class="blog-card-reading-time">5 min</span>`, sin autor.

Además, el owner señaló que la imagen de la tarjeta destacada se veía excesivamente grande. Se hizo un análisis de UX (medición de alturas reales sobre la captura del listado, 3 alternativas presentadas al owner con mockups ASCII) y se implementó la opción elegida: layout horizontal para la tarjeta destacada en escritorio. Ver design.md Decisión 7.

- [x] 7.2 Añadir un layout horizontal (`flex-direction: row`, imagen en columna fija del 42% con `min-height: 260px`, contenido en el resto) para `.blog-card-item--featured .blog-card` a partir de 769px de ancho de viewport, revirtiendo al layout vertical apilado por debajo de ese ancho (mismo punto de corte que ya usa `blog-grid` para pasar a una columna). Verificar con capturas Playwright en 390px, 768px y 1440px: por debajo de 769px la tarjeta destacada se ve apilada (imagen arriba, igual que antes); a 1440px, la tarjeta destacada baja de ~800px a ~330px de alto total, con imagen a la izquierda y contenido a la derecha. Verificado: capturas confirman ambos layouts; `npm run build` sin errores ni warnings nuevos.

Aun con el layout horizontal de la destacada, el owner observó que las tarjetas normales seguían ocupando mucho espacio en el listado completo (imagen + header + footer a la vista), y pidió compactar la escala tipográfica y el espaciado de todas las tarjetas, en dos pasadas (ver design.md Decisión 8).

- [x] 7.3 Añadir `max-height: 190px` a `.blog-card-media` (alineado con `.stage-banner`, 192px, ya usado en el sitio) y reducir `.blog-card-inner` (padding `1.75rem`→`1.5rem`, gap `0.75rem`→`0.6rem`) y la tipografía de la tarjeta en dos pasadas: primera pasada (título `1.1rem`→`1rem`, extracto `0.9rem`→`0.85rem`, meta `0.8rem`→`0.78rem`, CTA `0.82rem`→`0.8rem`, categoría `0.7rem`→`0.68rem`) y segunda pasada a petición del owner (título→`0.92rem`, extracto→`0.8rem`, meta→`0.72rem`, CTA→`0.76rem`, categoría→`0.64rem`, título/extracto de la destacada reducidos proporcionalmente). Verificar que el cambio, al vivir en el componente compartido, se aplica en las tres ubicaciones (listado `/blog`, portada "Últimas ideas", relacionados en `/blog/[slug]`) sin tocar esas páginas. Verificado con capturas Playwright en las tres ubicaciones tras la segunda pasada: mismo estilo compacto en las tres; tarjeta normal en ~430px de alto (frente a ~560px original); `npm run build` sin errores ni warnings nuevos.

A continuación, el owner pidió acordar el comportamiento sin imagen: en vez de un color por categoría, quiso un placeholder único alineado con la imagen de marca del sitio (ver design.md Decisión 3, revisada).

- [x] 7.4 Sustituir el marcador de posición (gradiente por categoría + icono genérico de imagen) por un placeholder de marca: fondo con el mismo degradado radial que `.hero-bg` de `global.css`, y el logo real del sitio (`public/logo.png`, el mismo `<img>` que usa `Header.astro`) centrado a 34px de alto con opacidad 0.55 (`alt=""` por ser decorativo). Verificar visualmente en las tres ubicaciones y en varios anchos (390/1440px) que el placeholder se ve consistente y legible. Verificado con capturas Playwright: logo reconocible y con contraste suficiente sobre el degradado en las tres ubicaciones y en mobile/desktop; `npm run build` sin errores.

El owner preguntó también por el checklist para publicar una imagen real de post (formato/tamaño, dónde colocar el archivo, cómo referenciarlo) — se respondió en la conversación (no requiere cambios de código) y se registró como regla de diseño a nivel de proyecto en `openspec/config.yaml` (`rules.design`) para que cualquier propuesta futura que haga `image` obligatorio en el schema de `blog` tenga en cuenta ese checklist y un plan de migración explícito para los posts existentes sin imagen. No se crea una tarea aquí porque es trabajo fuera del alcance de este cambio.

Como toque final de estilo, el owner pidió mover la fecha y el tiempo de lectura a una fila de cabecera junto a la categoría (categoría a un extremo, metadatos al otro), con la fecha acompañada de un emoji y el tiempo de lectura como badge (ver design.md Decisión 9). Esto cambia la secuencia de lectura descrita en `specs/blog/spec.md` (requisito "Estructura de contenido de la tarjeta de post"), actualizado en la misma sesión.

- [x] 7.5 Mover `<time>` (fecha, con emoji 📅 decorativo) y el badge de tiempo de lectura (emoji ⏱️ decorativo, `.blog-card-reading-badge` con fondo/borde propio) a un nuevo `.blog-card-header-row` junto a `.blog-card-category`, eliminando el bloque `.blog-card-meta`/`.blog-card-dot` que los mostraba debajo del extracto. Verificar que los emojis están marcados como decorativos (`aria-hidden="true"` en un `span` separado del texto real) y no forman parte del texto accesible del dato; verificar visualmente en las tres ubicaciones y en 390/1440px que categoría y metadatos quedan en la misma fila en desktop, y que en mobile, si no caben en una línea, la fila se envuelve sin romper el diseño. Verificado: `grep` sobre el HTML generado confirma la estructura `<span aria-hidden="true">📅</span> 8 de septiembre de 2026` (emoji fuera del nombre accesible); capturas Playwright confirman la fila de cabecera en desktop (categoría izquierda, fecha+badge derecha) y el envoltorio en dos líneas en mobile (390px); `npm run build` sin errores ni warnings nuevos.

El owner pidió cambiar el formato de fecha visible a DD/MM/YYYY (numérico), y preguntó si convenía adaptar el formato según la configuración regional del cliente (p. ej. mostrar MM/DD/YYYY si el navegador está en inglés/EE.UU.). Se descarta la detección de locale en cliente: el sitio es estático (sin hidratación) y la fecha es contenido esencial ya renderizado en build, por lo que personalizarla por visitante exigiría JS para un dato que hoy funciona sin él, entrando en conflicto con Q01 (contenido esencial sin JS) por un beneficio marginal en un sitio monolingüe es-ES. Se mantiene un formato único DD/MM/YYYY para todos los visitantes.

- [x] 7.6 Cambiar las opciones de `publishDate.toLocaleDateString('es-ES', ...)` en `src/components/BlogCard.astro` de `{ year: 'numeric', month: 'long', day: 'numeric' }` (p. ej. "8 de septiembre de 2026") a `{ year: 'numeric', month: '2-digit', day: '2-digit' }` (DD/MM/YYYY, p. ej. "08/09/2026"); el atributo `datetime` del `<time>` (ISO, vía `toISOString()`) no cambia. Verificado: `npm run build` sin errores; `grep` sobre `dist/blog/index.html` confirma `08/09/2026`, `06/08/2026` y `15/03/2026` para los tres posts existentes, sin ambigüedad respecto al formato americano (MM/DD/YYYY).
