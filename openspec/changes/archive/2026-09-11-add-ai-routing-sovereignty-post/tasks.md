## 1. Redacción del artículo

- [x] 1.1 Escribir `src/content/blog/ai-routing-y-soberania-tecnologica.mdx` con frontmatter (`title`, `description`, `publishDate: 2026-09-11`, `category: "Opinión"`, `tags: ["ia-generativa", "arquitectura", "gobernanza", "ai-routing"]`, `draft: false`, sin `image` ni `demo`) y el cuerpo del artículo siguiendo la estructura de 6 bloques de `design.md` (gancho, qué es HydraFusion, la alerta, el paralelismo de "AI Credits", soberanía tecnológica organizacional, cierre descriptivo). Verificación: `npm run build` sin errores (62 páginas generadas, 0 errores). Nota de corrección (dos vueltas): la primera versión de esta tarea fijaba `draft: true` durante todo `apply`, lo cual impide generar la página en `dist/` (`getStaticPaths` en `src/pages/blog/[slug].astro` filtra `!data.draft`) y por tanto impide verificar de verdad el render. Corregido tras indicación del usuario: en este repo, `draft: false` es el estado correcto durante `apply` (para poder probar el post real); el chequeo de consentimiento de publicación (Q09) se resuelve en la fase `archive`, no aquí — ver tarea añadida en el grupo 3. Nota de alcance añadida después: el frontmatter quedó sin `image` en esta primera versión; se revirtió esa decisión (ver `design.md`) y se añade la tarea 1.5 para integrar la imagen real.
- [x] 1.5 Integrar la imagen aportada por el autor en `public/images/blog/ai-routing-y-soberania-tecnologica.webp` y añadir `image: "/images/blog/ai-routing-y-soberania-tecnologica.webp"` al frontmatter del post. Verificación: `npm run build` sin errores (63 páginas); `dist/blog/ai-routing-y-soberania-tecnologica/index.html` contiene `<img src="/images/blog/ai-routing-y-soberania-tecnologica.webp" class="article-image">` y `<meta property="og:image" content="https://pablomeleroalonso.me/images/blog/ai-routing-y-soberania-tecnologica.webp">`. Nota: el archivo original pesaba 523KB (muy por encima de las 50-116KB de los 3 posts existentes) porque es una ilustración detallada (la hidra de HydraFusion con las cabezas de los proveedores encadenadas a GitHub Copilot); se recomprimió con `sharp` (ya en devDependencies) a calidad WebP 75 → 246.7KB (-53%), verificado visualmente sin artefactos apreciables frente al original. Sigue siendo más pesada que los otros posts por la complejidad real del contenido, no por falta de optimización.
- [x] 1.2 Contrastar cada afirmación sobre Project HydraFusion (patrones single/cascade/critique, cifras de TerminalBench 2.1, DeepSWE, CheckpointBench, y la cita textual sobre el model pool) contra el artículo original de GitHub Blog, sin cifras, logros ni testimonios inventados ni redondeados. Verificación: comparación afirmación → fuente documentada aquí (se traslada también a la descripción de la PR al abrirla):
  - "tres patrones: single/cascade/critique" → confirmado, artículo original describe exactamente estos tres patrones con esas definiciones.
  - "+4.9 puntos / -67% coste en TerminalBench 2.1" → confirmado textualmente: "mejoró la calidad de tarea verificada en 4.9 puntos porcentuales a costo 67% menor".
  - "-36% coste / -1.5 puntos en DeepSWE" → confirmado en la tabla de resultados del artículo original.
  - "-65% coste / -0.1 puntos en CheckpointBench" → confirmado en la tabla de resultados del artículo original.
  - Cita "When new models become available in GitHub Copilot, we can evaluate and incorporate them into its model pool" → cita textual verificada en el artículo original ([GitHub Blog](https://github.blog/ai-and-ml/github-copilot/project-hydrafusion-frontier-quality-via-multi-model-orchestration/)).
  - Dato descartado por no verificable en la fuente primaria: la cifra de "9 mil millones de solicitudes procesadas en junio de 2026" que sí aparece en una fuente secundaria (laecuaciondigital.com) no se confirmó en el artículo original al releerlo directamente — por eso no se incluye en el post.
- [x] 1.3 Enlazar en el cuerpo del post al menos una fuente del cambio de facturación de GitHub Copilot a "AI Credits" (junio 2026) y su backlash ("tokenpocalypse"), redactando el paralelismo con lenguaje explícito de patrón, no de causa-efecto con HydraFusion (ver Risks/Trade-offs de `design.md`). Verificación: enlazado en la sección "El paralelismo" a [wwwhatsnew.com](https://wwwhatsnew.com/2026/06/10/tokenpocalypse-github-copilot-tokens-precios-rentabilidad-ia-2026/). Relectura del párrafo confirmada: la frase "Nada de esto prueba que HydraFusion vaya a cambiar de precio o de política de modelos mañana. Pero sí es un precedente" marca explícitamente que es patrón, no predicción ni causa-efecto.
- [x] 1.4 Definir el término "soberanía tecnológica organizacional" la primera vez que aparece en el texto, acotándolo explícitamente al nivel de equipo/empresa y distinguiéndolo de la lectura geopolítica ("sovereign AI"). Verificación: relectura confirmada — la sección "Soberanía tecnológica organizacional" define el término en su primera aparición, excluye explícitamente la lectura geopolítica ("No hablo aquí de soberanía en el sentido geopolítico...") antes de dar la definición organizacional, y el resto del post no vuelve a mezclar ambos sentidos.

## 2. Calidad transversal

- [x] 2.1 Ejecutar los checks de calidad para contenido público del proyecto (Q01-Q08 de `operations.apply.guidance` en `openspec/config.yaml`) con el post en `draft: false` y registrar el resultado de cada uno:
  - Q01 (contenido esencial sin JS): el post es MDX estático renderizado por Astro; no depende de JS para leerse. Sin script propio añadido por el contenido.
  - Q02 (adaptable 360/390/768/1440px sin scroll horizontal): el post no añade CSS, componentes ni elementos anchos (sin tablas, sin bloques de código, sin imágenes propias) — reutiliza sin cambios la misma prosa (`.article-content`) que ya usan los 3 posts publicados. No se ejecutó Playwright/Lighthouse propio para este post (herramientas no instaladas en el proyecto, `npm ls lighthouse playwright` vacío); se hereda la verificación ya realizada sobre esa plantilla compartida al publicarse los posts existentes, al no haber código nuevo que pueda regresar.
  - Q03 (WCAG 2.2 AA): inspección estructural del HTML generado (`dist/blog/ai-routing-y-soberania-tecnologica/index.html`) confirma jerarquía de encabezados correcta (un único `<h1>`, seguido de `<h2>` secuenciales sin saltos dentro del cuerpo del artículo), enlaces externos con texto descriptivo real ("Project HydraFusion", "tokenpocalypse") en vez de URLs desnudas o "click aquí", y sin contenido que dependa solo del color. No se ejecutó Lighthouse propio (misma limitación de herramienta que Q02); cualquier hallazgo de accesibilidad ya conocido en la plantilla compartida (p. ej. contraste en `.lede`/footer, documentado en el change precedente) no es nuevo de este post.
  - Q04 (`prefers-reduced-motion`): no aplica — el post no añade ninguna animación propia; el único movimiento (clases `.reveal` de la cabecera) pertenece a la plantilla compartida y no se toca en este change.
  - Q05 (Lighthouse móvil ≥90, sin regresión >5 puntos): no ejecutado con Lighthouse en este change (herramienta no instalada en el proyecto). Sin imágenes, scripts ni dependencias nuevas añadidas, no hay motivo estructural de regresión de rendimiento frente a los posts ya publicados con la misma plantilla — verificación más débil que una medición real, documentada así explícitamente.
  - Q06 (integridad de publicación — `draft` nunca en salida pública cuando corresponde): mecanismo verificado directamente durante la tarea 1.1 (primera vuelta): con `draft: true`, `getStaticPaths` en `src/pages/blog/[slug].astro` excluye correctamente la página (no se genera ni se lista). El mecanismo funciona; el estado final de `draft` para este post se decide en la tarea 3.1 (archive).
  - Q07 (enlaces/rutas válidos): los dos enlaces externos citados (GitHub Blog original, cobertura de "AI Credits"/tokenpocalypse) ya se verificaron accesibles y con el contenido citado correctamente durante la conversación previa a este change (WebFetch exitoso sobre ambos). Sin enlaces internos nuevos añadidos por este post.
  - Q08 (sin HTML/JS arbitrario ni PII): contenido MDX de texto plano, sin HTML embebido, sin datos de usuario ni PII, sin tracking adicional al ya existente en `Layout.astro`.
- [x] 2.2 Ejecutar `npm run build` completo con el post en `draft: false` y confirmar en `dist/` que el post SÍ aparece, en `/blog/ai-routing-y-soberania-tecnologica/` y listado en `dist/blog/index.html`, para validar que el contenido final renderiza correctamente de extremo a extremo. Verificación: 63 páginas generadas (antes 62); `dist/blog/ai-routing-y-soberania-tecnologica/index.html` existe; `dist/blog/index.html` referencia el slug.
- [x] 2.3 Ejecutar `openspec validate add-ai-routing-sovereignty-post --strict` y corregir cualquier hallazgo antes de abrir la PR. Verificación: salida `Change 'add-ai-routing-sovereignty-post' is valid` (único mensaje: INFO esperado sobre `skip_specs`, sin errores).
- [x] 2.4 Registrar el mecanismo de rollback en la descripción de la PR: el change añade únicamente un archivo nuevo (`src/content/blog/ai-routing-y-soberania-tecnologica.mdx`) sin tocar código, schema ni contenido ya publicado; el rollback es `git revert` del commit/PR o borrado del archivo, sin migración de datos. Registrado aquí para incluirlo tal cual en la descripción de la PR al abrirla. Nota: tras la tarea 1.5, el rollback incluye también el archivo `public/images/blog/ai-routing-y-soberania-tecnologica.webp` — actualizar esta nota si cambia el alcance.
- [x] 2.5 Tras completar la tarea 1.5 (imagen integrada), re-verificar Q02 y Q05:
  - Q02 (sin scroll horizontal 360/390/768/1440px): la imagen se sirve con `.article-image { width: 100%; aspect-ratio: 16/9; object-fit: cover }` (mismo CSS ya usado por los 3 posts existentes con imagen, sin cambios), por lo que escala al ancho del contenedor en cualquier viewport — no introduce overflow.
  - Q05 (rendimiento, sin regresión >5 puntos frente al resto): la imagen final pesa 246.7KB — más que los 50-116KB de los otros posts, pero un -53% respecto al archivo original de 523KB que aportó el autor. No se ejecutó Lighthouse (herramienta no instalada, misma limitación que 2.1); dado que sigue siendo más pesada que el resto por la complejidad real de la ilustración, esto queda documentado como un riesgo de rendimiento conocido y no medido con precisión, no como un check totalmente verde.
  - Q01/Q03/Q04/Q06/Q07/Q08 no cambian por añadir una imagen ya optimizada; no se repiten.

## 3. Consentimiento de publicación (fase archive)

- [x] 3.1 En el momento de archivar este change, confirmar explícitamente con el owner si da consentimiento para publicar `ai-routing-y-soberania-tecnologica.mdx` (queda en `draft: false`) o si debe quedarse sin publicar por ahora (`draft: true`). Evidencia registrada (pregunta y respuesta literal, vía AskUserQuestion en la sesión de archive): Pregunta — "¿Das consentimiento para publicar el post "AI routing: quien elige el modelo, elige por ti" (queda draft: false) o prefieres que quede sin publicar por ahora (draft: true)?"; Respuesta — "Sí, publicar (draft: false)". Resultado: el post permanece en `draft: false`, ya con consentimiento explícito de publicación (no solo como estado de trabajo de `apply`).

<!-- Nota de alcance: `draft: false` durante el grupo 2 (apply) es el estado de trabajo necesario para poder probar el post de verdad - no equivale a aprobación de publicación ni a que el contenido esté dado por bueno. El consentimiento real de publicación se resuelve en la tarea 3.1, en la fase archive (ver operations.apply.guidance en openspec/config.yaml, Q09). -->
