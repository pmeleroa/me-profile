## 1. Redacción del artículo

- [x] 1.1 Escribir `src/content/blog/sdd-vs-vibe-coding.mdx` con frontmatter (`title`, `description`, `publishDate`, `category`, `tags`, `draft: true`, `demo: { slug: "telefono-roto" }`) y el cuerpo del artículo (tesis de las dos trazabilidades independientes, evidencia de los 4 changes archivados, ejemplo ficticio de la demo señalado explícitamente como ilustrativo, separado de la evidencia real). Verificación: `npm run build` sin errores + inspección de `dist/` confirmando que la página del post se genera y enlaza a `/lab/telefono-roto`.
- [x] 1.2 Contrastar cada afirmación del artículo sobre los 4 changes citados (`upgrade-astro-v7`, `upgrade-github-actions`, `upgrade-sharp-libvips-security`, `add-lab-interactive-demos`) contra su `proposal.md` real en `openspec/changes/archive/`, sin cifras, logros ni testimonios inventados. Verificación: comparación documentada en la descripción de la PR (afirmación → fuente).

## 2. Demo interactiva `/lab/telefono-roto`

- [x] 2.1 Escribir el guion completo de las 4 rondas x 2 ramas (mensaje de cada ronda, opciones, cuál opción causa deriva) como contenido, antes de codificar, siguiendo las decisiones de `design.md`. Verificación: guion releído comprobando que ninguna opción que causa deriva resulta obviamente descuidada en el momento de elegirla.
  - **Nota post-implementación**: la primera versión del guion (con la que se marcó esta tarea durante `/opsx:apply`) sí incluía frases que delataban tanto la opción de deriva (`"JSON queda como TODO para más adelante"`, `"se retoma después"`) como la opción segura por el lado contrario (`"tal como pide el requisito"`, `"sin tocar qué formatos soporta"`, `"sin tocar los formatos que ya cubre"`). Tras revisión del usuario, se reformularon las 6 opciones (3 de deriva + 3 seguras) para que ninguna se autoseñale — ver `src/pages/lab/telefono-roto.astro`, array `ROUNDS`, para el guion final. Reverificado con el mismo test funcional (Playwright, 17/17) tras cada ronda de cambios.
- [x] 2.2 Crear `src/pages/lab/telefono-roto.astro` reutilizando el esqueleto de `src/pages/lab/test-memoria.astro` (tokens de `global.css`, enlace único de vuelta al post, sin navegación principal del sitio). Verificación: `npm run build` + inspección de `dist/` confirmando que la ruta `/lab/telefono-roto` se genera.
- [x] 2.3 Implementar la rama "sin contrato visible" (estado en JS cliente, requisito mostrado solo al inicio, 4 rondas secuenciales, pantalla final comparando el estado resultante con el requisito original). Verificación: recorrido manual completo en `npm run dev` confirmando que el resultado final muestra la deriva esperada.
- [x] 2.4 Implementar la rama "con contrato visible" (mismo guion de rondas, contrato de una línea fijo en pantalla durante todo el recorrido, marcado visual de las opciones que lo violarían). Verificación: recorrido manual completo en `npm run dev` confirmando que el resultado final coincide con el requisito original.
- [x] 2.5 Confirmar la asociación estructurada entre el post y la demo mediante el campo `demo` (frontmatter, tarea 1.1) y que el enlace de vuelta de la demo apunta al post. Verificación: navegación manual ida y vuelta post ↔ demo en `npm run dev`.

## 3. Calidad transversal y publicación

- [x] 3.1 Ejecutar los checks de calidad para contenido/UI público del proyecto. Resultado de cada uno:
  - Q01 (contenido esencial sin JS): el post es MDX estático (no depende de JS). La demo degrada a la pantalla inicial (requisito + intro) visible sin JS, igual que `test-memoria` — mismo patrón ya aceptado en el sitio.
  - Q02 (adaptable 360/390/768/1440px sin scroll horizontal): verificado con Playwright en los 4 anchos, en la pantalla más densa (decisión con banner de contrato) — sin scroll horizontal en ninguno.
  - Q03 (WCAG 2.2 AA): Lighthouse reporta accessibility=88, con 2 fallos (`color-contrast` en `.lab-back-link`/`.lede`/`footer`, `landmark-one-main`). Verificado que los mismos 2 fallos, con los mismos elementos/tokens, ya existen en `/lab/test-memoria` (producción) — provienen de `global.css`/`Layout.astro`/`LabBackLink.astro` compartidos, no de código nuevo de este change. Decisión (confirmada con el usuario): documentar sin bloquear esta PR; el arreglo de fondo (contraste de tokens compartidos, `<main>` en `Layout.astro`) queda para un change aparte que toque esos archivos compartidos.
  - Q04 (`prefers-reduced-motion`): verificado con Playwright (`emulateMedia`) que el `transform` de hover queda en `none` con la preferencia activada.
  - Q05 (Lighthouse móvil ≥90, sin regresión >5 puntos): performance=72, igual que el baseline de `/lab/test-memoria` (72) — cero regresión, pero no alcanza el umbral absoluto de 90. Mismo origen que Q03 (LCP alto, compartido con el patrón `/lab/` existente, no nuevo). Misma decisión: documentar, no bloquear.
  - Q06 (`draft` nunca en salida pública): verificado en la tarea 1.1 (con `draft: true`, `npm run build` no genera `dist/blog/sdd-vs-vibe-coding/`).
  - Q07 (enlaces/rutas válidos): verificado con Playwright — post→demo y demo→post resuelven correctamente en ambas direcciones.
  - Q08 (sin HTML/JS arbitrario ni PII): la demo no toma texto libre del usuario (solo botones/checkboxes controlados por el guion), no hay `innerHTML` con datos no confiables ni tracking de PII.
- [x] 3.2 Ejecutar `npm run build` completo con el post en `draft: true` y confirmar en `dist/` que ni el post ni la demo aparecen en el listado público del blog. Verificación: `dist/blog/sdd-vs-vibe-coding/` no existe y `dist/blog/index.html` no contiene ninguna referencia al post. Nota: el proyecto no tiene integración de sitemap (`@astrojs/sitemap` no está en `package.json`, no hay `sitemap` en `astro.config.mjs`) — no existe una sitemap que inspeccionar; el resto del proyecto tampoco genera una. La demo `/lab/telefono-roto` sí se genera (el espacio `/lab/` no tiene concepto de `draft`, igual que `test-memoria`), pero solo es alcanzable navegando la URL directamente o desde el post, que sigue sin publicar.
- [x] 3.3 Ejecutar `openspec validate add-sdd-vs-vibe-coding-post --strict` y corregir cualquier hallazgo antes de abrir la PR. Verificación: `Change 'add-sdd-vs-vibe-coding-post' is valid` (sin errores; único mensaje informativo esperado sobre `skip_specs`).
- [x] 3.4 Registrar el mecanismo de rollback en la descripción de la PR: el change añade únicamente archivos nuevos (`src/content/blog/sdd-vs-vibe-coding.mdx`, `src/pages/lab/telefono-roto.astro`) sin tocar código, schema ni contenido ya publicado; el rollback es `git revert` del commit/PR o borrado de ambos archivos, sin migración de datos. Verificación: mecanismo descrito aquí y se incluirá en la descripción de la PR al abrirla.

<!-- Nota de alcance: ninguna tarea marca `draft: false` ni publica el post — completar esta lista implica implementación terminada, no contenido aprobado ni publicación realizada (ver operations.apply.guidance en openspec/config.yaml). -->
