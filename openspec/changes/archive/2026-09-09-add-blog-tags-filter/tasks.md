## 1. Filtrado combinado en el listado de posts

- [x] 1.1 Calcular en `index.astro` la lista de tags disponibles y su conteo por tag (sobre todos los posts publicados); verificar con `npm run build` que el listado compila sin errores. **Verificado**: `npm run build` → 62 páginas generadas sin errores; `dist/blog/index.html` contiene 6 `data-tag` distintos (arquitectura, ia-generativa, ingenieria, memoria, productividad, sdd).
- [x] 1.2 Implementar en el script cliente de `index.astro` el filtrado combinado (tags activos en OR entre sí, y en AND con la categoría activa); verificar con `npm run dev` activando categoría + varios tags a la vez y comprobando que el listado resultante coincide con la combinación esperada. **Verificado**: script Playwright (`scratchpad/verify.mjs`) contra `npm run dev` — activar tag `sdd` deja 1 tarjeta visible; combinar categoría `IA Generativa` + tag `sdd` mantiene 1 visible (AND correcto). 9/9 checks PASS.
- [x] 1.3 Mostrar un mensaje de "sin resultados" cuando la combinación de filtros no coincide con ningún post; verificar activando una combinación de categoría + tag que no exista en ningún post y comprobando que aparece el mensaje en vez de un grid vacío. **Verificado**: `/blog?category=DevOps&tags=sdd` (DevOps no tiene posts) → `#blog-no-results` visible, 0 tarjetas visibles (check 5, PASS).

## 2. Estado de los filtros en la URL

- [x] 2.1 Leer `category` y `tags` desde los query params al cargar `/blog` y aplicar el filtrado inicial en consecuencia; verificar abriendo `/blog?category=IA+Generativa&tags=sdd` directamente y comprobando que el listado ya sale filtrado sin interacción previa. **Verificado**: check 4 del script Playwright, PASS.
- [x] 2.2 Actualizar la URL (sin recargar la página) al activar/desactivar categoría o tags, usando `history.pushState`/`replaceState`; verificar en el navegador que la barra de direcciones refleja los filtros activos tras clicar chips. **Verificado**: check 3b, `page.url()` tras click en chip `sdd` → contiene `tags=sdd`, PASS.
- [x] 2.3 Escuchar el evento `popstate` para resincronizar el estado visual de los filtros al navegar con los botones atrás/adelante del navegador; verificar activando varios filtros en secuencia y comprobando que el botón "atrás" restaura el estado anterior visualmente. **Verificado**: check 8 — tras activar categoría + tag `memoria` y pulsar atrás, la URL vuelve a solo `category=IA+Generativa` y el chip de categoría queda `aria-pressed=true`, PASS.

## 3. Panel colapsable de tags

- [x] 3.1 Añadir el control "Filtrar por tags" y el panel colapsable bajo la fila de categoría en `index.astro`, cerrado por defecto; verificar visualmente (`npm run dev`, sin query params) que la altura del hero no aumenta respecto al comportamiento actual. **Verificado**: check 1 (`#tags-panel` oculto por defecto, PASS) + captura de pantalla (`scratchpad/blog-panel-open.png`) confirmando que el hero se mantiene compacto con el toggle colapsado.
- [x] 3.2 Auto-expandir el panel y marcar como activos los tags presentes en la URL al cargar la página; verificar abriendo `/blog?tags=sdd` y comprobando que el panel aparece ya desplegado con el chip `sdd` marcado. **Verificado**: check 6 — `panelExpanded=true`, `chipActive=true`, `visibles=1`, PASS.
- [x] 3.3 Mostrar el número de tags activos en el control cuando el panel está colapsado; verificar activando 2 tags y colapsando el panel manualmente, comprobando que el control muestra el conteo (p. ej. "Filtrar por tags (2)"). **Verificado**: script dedicado (`scratchpad/verify-count.mjs`) — con `?tags=sdd,memoria` y panel colapsado manualmente, el label muestra "Filtrar por tags (2)", PASS.

## 4. Navegación desde la página de artículo

- [x] 4.1 Convertir los `<span class="article-tag">` de `[slug].astro` en enlaces `<a>` hacia `/blog?tags=<tag>`; verificar haciendo click en un tag de un artículo publicado y comprobando que se navega a `/blog` con ese tag activo y el panel de tags ya expandido. **Verificado**: check 7 — click en `#arquitectura` desde `/blog/sdd-vs-vibe-coding` navega a `/blog?tags=arquitectura` con el panel expandido, PASS.

## 5. Verificación integral y rollback

- [x] 5.1 Ejecutar `npm run build` e inspeccionar `dist/blog/` para confirmar que el listado y las páginas de artículo se generan sin errores; comando: `npm run build`, resultado esperado: build exitoso, sin errores ni warnings nuevos. **Verificado**: `npm run build` → "62 page(s) built", sin errores; `dist/blog/{index.html, ia-en-ingenieria-de-software/, perdidos-en-el-medio/, sdd-vs-vibe-coding/}` presentes.
- [x] 5.2 Ejecutar `openspec validate add-blog-tags-filter --strict` y confirmar que no reporta errores. **Verificado**: `openspec validate add-blog-tags-filter --strict` → "Change 'add-blog-tags-filter' is valid".
- [x] 5.3 Registrar el mecanismo de rollback: dado que `src/pages/blog/index.astro` y `src/pages/blog/[slug].astro` son código ya en producción, revertir mediante `git revert` del/los commit(s) de este change (o `git checkout <commit-anterior> -- src/pages/blog/index.astro src/pages/blog/[slug].astro`) si tras el despliegue se detecta una regresión. **Registrado**: rollback vía `git revert <commit-de-este-change>` sobre `feat/add-blog-tags-filter` (o su squash merge en `main`), sin migración de datos que deshacer — `content.config.ts` y el frontmatter de los posts no se han modificado.

## 6. Refinamiento post-prueba local: desplegable con cierre automático

- [x] 6.1 Reposicionar el control "Filtrar por tags" a la derecha del filtro de categoría, en la misma fila (`.filters-row`), en vez de en una fila propia debajo; verificar con Playwright que el bounding box del toggle queda a la derecha del bounding box del filtro de categoría y en la misma línea vertical. **Verificado**: check "Toggle de tags a la derecha del filtro de categoría (misma fila)" → PASS (`cat.x=449`, `tags.x=688`, misma `y`).
- [x] 6.2 Cerrar el panel de tags automáticamente al interactuar fuera de su contexto (click en cualquier otro punto de la página, incluida la selección de otro filtro) o al pulsar Escape, sin exigir que el visitante lo cierre explícitamente; seleccionar un tag dentro del panel no debe cerrarlo. Verificar con Playwright los tres casos. **Verificado**: checks "Click fuera del combo lo cierra", "Click en chip de categoría (fuera del combo) también lo cierra", "Escape cierra el combo" y "Click en un tag dentro del panel NO lo cierra" → 4/4 PASS.
- [x] 6.3 Corregir el bug de *stacking context* detectado al posicionar el panel como `position: absolute`: al estar anidado en un ancestro con la clase `.reveal` del sitio (que aplica `transform`), el `z-index` del panel quedaba aislado y las tarjetas del listado lo tapaban, interceptando los clicks sobre los chips de tag; se corrigió dando a `.blog-header` su propio `position: relative; z-index: 1`. Verificar que el click en un chip de tag dentro del panel abierto ya no falla por intercepción de otro elemento. **Verificado**: `elementFromPoint` sobre el centro del chip `#sdd` con el panel abierto devuelve el propio `<button data-tag="sdd">` (antes devolvía `.blog-card-inner`); regresión completa re-ejecutada tras el fix — 9/9 (suite original) + 6/6 (dropdown) + verificación de conteo "(2)" → 16/16 PASS, sin errores de consola, `npm run build` sin errores (62 páginas).
- [x] 6.4 Sustituir el fondo translúcido (`var(--color-surface)`, pensado para tarjetas apoyadas sobre el fondo de página) del panel flotante por un fondo sólido (`var(--color-bg-2)`), ya que al superponerse a contenido arbitrario dejaba ver el texto de detrás y confundía. Verificar visualmente que el título del artículo detrás del panel deja de transparentarse. **Verificado**: captura de pantalla (`scratchpad/blog-panel-opaque.png`) con el panel abierto sobre la tarjeta destacada — el texto queda completamente oculto detrás del panel.
- [x] 6.5 Corregir el modelo de historial de navegador para la selección de tags: marcar varios tags en una misma apertura del panel debía colapsar en un solo paso de "atrás" (no uno por tag), evaluando explícitamente si convenía un botón "Aplicar" explícito y descartándolo por chocar con el cierre automático ya implementado (aplicar-al-cerrar vs descartar-al-cerrar son ambos peores que el filtrado en vivo). Se implementó un modelo de "sesión": el primer tag marcado tras abrir el panel hace `pushState` (preserva el estado previo a abrir el panel), los tags siguientes de esa misma sesión hacen `replaceState`; un cambio de categoría posterior sigue siendo su propio `pushState` independiente. Verificar con Playwright, usando el contenido de la URL en cada paso de "atrás" (no el conteo de `history.length`, que un `goBack()` previo puede distorsionar por truncamiento de entradas "colgantes"). **Verificado**: suite dedicada (`scratchpad/verify-history2.mjs`) — 7/7 PASS: 3 tags seguidos + 1 "atrás" vuelve a `/blog` limpio; tag + cambio de categoría son 2 "atrás" independientes (1o deshace categoría, 2o deshace tags); abrir/cerrar sin cambios no toca la URL. Regresión completa re-ejecutada tras el fix — 9/9 + 6/6 + conteo, sin errores de consola; `npm run build` sin errores (62 páginas).
- [x] 6.6 Quitar la auto-expansión del panel al llegar a una URL con tags activos (carga inicial, click en un tag del artículo, y navegación atrás/adelante): el panel debe permanecer siempre colapsado por defecto, aunque el listado ya salga filtrado y el control ya muestre el conteo. Verificar con Playwright que `/blog?tags=sdd` en acceso directo, el click en un tag del artículo, y un `popstate` hacia un estado con tags, dejan el panel colapsado (no solo el caso de acceso directo). **Verificado**: suite dedicada (`scratchpad/verify-no-autoexpand.mjs`) — 8/8 PASS: panel colapsado + listado filtrado + chip activo + conteo "(1)" en acceso directo; mismo resultado llegando desde el enlace de un tag del artículo; apertura manual sigue funcionando; tras `popstate` hacia un estado con tags el panel también queda colapsado. `npm run build` sin errores (62 páginas).
