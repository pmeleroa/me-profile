## 1. Datos y schema

- [x] 1.1 Crear `src/data/author.ts` con el dato de autor hoy hardcodeado en `[slug].astro` (nombre, avatar, biografía, enlaces de LinkedIn/GitHub), exportado como un objeto tipado que pueda ser `null`/`undefined` para representar "sin autor". Verificado: `npx astro check` requiere instalar `@astrojs/check`/`typescript` (no presentes, no se añaden por ser dependencia nueva fuera de alcance); verificado en su lugar con `npm run build` → `62 page(s) built` sin errores.
- [x] 1.2 Añadir a `src/content.config.ts` los campos opcionales `ctaLabel`, `ctaText`, `ctaHref` (`z.string().optional()`) al schema de la colección `blog`, sin tocar los campos existentes ni su obligatoriedad. Verificado con `npm run build` → `62 page(s) built` sin error de validación de schema; los 3 posts existentes (sin estos campos) compilan igual.

## 2. Layout compartido

- [x] 2.1 Extender `src/layouts/Layout.astro` con los props opcionales `type?: 'website' | 'article'` (por defecto `'website'`), `publishedTime?: string`, `modifiedTime?: string`, y añadir `<meta property="og:type">` dinámico más las meta `article:published_time`/`article:modified_time` cuando `type === 'article'`. Verificado: `npm run build` → `62 page(s) built`; `dist/index.html` y `dist/contacto/index.html` muestran `<meta property="og:type" content="website">` y 0 coincidencias de `article:` en `dist/index.html`.
- [x] 2.2 Añadir `<slot name="head" />` en `Layout.astro` justo antes de `</head>`. Verificado: el build sigue generando 62 páginas sin error; ninguna página salvo el artículo pasa contenido al slot `head`, así que su `<head>` no cambia (slot vacío no renderiza nada).
- [x] 2.3 Rollback si algo falla en este grupo: `git checkout -- src/layouts/Layout.astro` (código ya usado en producción por todas las páginas del sitio). No hizo falta invocarlo: 2.1 y 2.2 verificaron en verde.

## 3. Reestructuración de la página de artículo

- [x] 3.1 Reordenar `src/pages/blog/[slug].astro` a la secuencia: breadcrumb, categoría + tags, título, bajada, metadatos (fecha + tiempo de lectura), imagen destacada, contenido, autor, relacionados, CTA final. Verificado con `npm run build` + inspección de `dist/blog/sdd-vs-vibe-coding/index.html` (orden confirmado por regex sobre el HTML) y con `npm run dev` sirviendo la misma URL (HTTP 200); las clases `reveal`/`reveal-delay-1..4` se aplican en cascada a breadcrumb → taxonomía → título → bajada → metadatos, dentro del presupuesto de delays ya definido en `global.css`.
- [x] 3.2 Añadir el breadcrumb (`nav aria-label="Breadcrumb"` + `ol`) con los niveles Inicio, Blog, categoría (enlace a `/blog?category=<categoría>`) y título (`aria-current="page"`, no enlazado). Verificado en `dist/blog/sdd-vs-vibe-coding/index.html`: `<a href="/">Inicio</a>`, `<a href="/blog">Blog</a>`, `<a href="/blog?category=IA%20Generativa">IA Generativa</a>`, y `<li aria-current="page">SDD vs vibe coding...</li>` sin enlace — mismo mecanismo de query param ya usado por los tags.
- [x] 3.3 Calcular el tiempo de lectura del post principal con `readingTime(post.body)` (ya usado para los relacionados) y mostrarlo junto a la fecha de publicación en el bloque de metadatos. Verificado en `dist/blog/sdd-vs-vibe-coding/index.html`: `<span class="article-reading-time">5 min de lectura</span>` junto a la fecha, mismo valor que ya calculaba `BlogCard` para este post como relacionado.
- [x] 3.4 Mostrar "Actualizado el {fecha}" junto a la fecha de publicación solo cuando `post.data.updatedDate` está definido. Verificado el caso "sin dato": los 3 posts reales (sin `updatedDate`) no muestran ningún texto de actualización en `dist/blog/sdd-vs-vibe-coding/index.html`. Caso "con dato" verificado por inspección de código, no por ruta renderizada (ver nota en 6.2): `formattedUpdatedDate = post.data.updatedDate ? dateFormatter.format(...) : undefined` en `[slug].astro`, y el fixture de 6.1 define `updatedDate: 2026-09-10`, por lo que `{formattedUpdatedDate && (...)}` resuelve verdadero y renderiza "Actualizado el 10 de septiembre de 2026".
- [x] 3.5 Renderizar la imagen destacada (`post.data.image`) en proporción `16:9` con `alt={post.data.title}` cuando existe, y omitir el bloque completo cuando no existe. Verificado el caso "sin imagen": `grep -c "article-image-wrap" dist/blog/sdd-vs-vibe-coding/index.html` → `0`, sin hueco ni placeholder. Caso "con imagen" verificado por inspección de código (ver nota en 6.2): `{post.data.image && (...)}` en `[slug].astro`, y el fixture define `image: "/og-image.png"`, por lo que renderiza `<img src="/og-image.png" alt="Cómo Crear Una Estrategia De Contenidos Que Genere Tráfico" class="article-image">` con `aspect-ratio: 16/9` ya fijado en el CSS de `.article-image`.
- [x] 3.6 Sustituir el bloque de autor hardcodeado por el import de `src/data/author.ts`, omitiendo la sección `author-section` por completo cuando el dato es `null`/`undefined`. Verificado en `dist/blog/sdd-vs-vibe-coding/index.html`: nombre "Pablo Melero Alonso", bio y enlaces LinkedIn/GitHub idénticos al contenido anterior al cambio.
- [x] 3.7 Hacer la CTA final configurable leyendo `post.data.ctaLabel`/`ctaText`/`ctaHref` con fallback al texto y enlace de contacto actuales cuando el post no los define. Verificado en `dist/blog/sdd-vs-vibe-coding/index.html` (post sin estos campos): `<p class="article-cta-text">¿Te resultó útil? Me interesa saber qué piensas.</p>` + `<a href="/contacto" class="btn-primary">Hablemos</a>`, igual que antes del cambio. Caso "con CTA propia" verificado por inspección de código (ver nota en 6.2): `ctaText = post.data.ctaText ?? '...'` con el fixture definiendo `ctaText: "Suscríbete para recibir nuevas guías de marketing cada semana."`, `ctaLabel: "Suscribirme"`, `ctaHref: "/contacto"` — el operador `??` usa el valor del post en vez del valor por defecto.
- [x] 3.8 Rollback si algo falla en este grupo: `git checkout -- src/pages/blog/[slug].astro` (página ya en producción). No hizo falta invocarlo: 3.1–3.3, 3.6–3.7 verificaron en verde.

## 4. SEO estructurado

- [x] 4.1 En `[slug].astro`, pasar `type="article"`, `publishedTime={post.data.publishDate.toISOString()}` y, si existe, `modifiedTime={post.data.updatedDate.toISOString()}` a `Layout`. Verificado en `dist/blog/sdd-vs-vibe-coding/index.html`: `<meta property="og:type" content="article">` y `<meta property="article:published_time" content="2026-09-08T00:00:00.000Z">`.
- [x] 4.2 Añadir en el slot `head` de la página un `<script type="application/ld+json">` con `BlogPosting` (headline, description, image si existe, datePublished, dateModified si existe, author, mainEntityOfPage) construido a partir de las mismas variables que ya alimentan el HTML visible (sin literales duplicados). Verificado extrayendo el bloque con Python y `json.loads` sobre el contenido de `dist/blog/sdd-vs-vibe-coding/index.html`: JSON válido con `headline`, `description`, `datePublished`, `author.name` y `mainEntityOfPage` coincidentes con el HTML visible (sin `image`, porque este post no define `post.data.image`, tal y como especifica la tarea).
- [x] 4.3 Añadir en el mismo slot un `<script type="application/ld+json">` con `BreadcrumbList` cuyos niveles coincidan exactamente con los del breadcrumb visible (tarea 3.2). Verificado: el segundo bloque `application/ld+json` en `dist/blog/sdd-vs-vibe-coding/index.html` tiene 4 `itemListElement` con los mismos 4 niveles (Inicio, Blog, IA Generativa, título) y las mismas URLs que los `href` del breadcrumb visible.

## 5. Accesibilidad

- [x] 5.1 Añadir `:focus-visible` (con el mismo tratamiento visual que ya usa `BlogCard` — `outline: 2px solid var(--color-accent); outline-offset: 3px;`) a los tags del artículo, los enlaces del bloque de autor, los niveles enlazados del breadcrumb y el botón de la CTA final, que hoy solo tienen `:hover`. Verificado en el CSS compilado (`dist/_astro/_slug_.*.css`): 5 reglas `:focus-visible{outline:2px solid var(--color-accent);outline-offset:3px}` (breadcrumb, tag, author-link, btn-primary, más la heredada del contenido enlazado del artículo).

## 6. Verificación con contenido de ejemplo (no versionado)

- [x] 6.1 Crear localmente (sin `git add`) `src/content/blog/como-crear-estrategia-contenidos.mdx` con los datos del pedido: título "Cómo Crear Una Estrategia De Contenidos Que Genere Tráfico", bajada "Una guía práctica para planificar, publicar y medir contenidos que atraigan lectores cualificados de forma constante.", `publishDate: 2026-09-09`, `updatedDate: 2026-09-10`, tags de ejemplo, `ctaText`/`ctaLabel`/`ctaHref` de ejemplo, `image: "/og-image.png"` (asset ya existente en `public/`, reutilizado solo para esta verificación), `draft: true`, y `category: "Opinión"` (una de las 5 categorías ya existentes; no se añade "Marketing" — ver `design.md`, Decisiones). Verificado con `npm run build` → `62 page(s) built` sin errores de schema.
- [x] 6.2 Hallazgo que cambia el método de verificación previsto: `getStaticPaths` en `[slug].astro` filtra `!data.draft`, igual que ya hace `blog/index.astro` — un post `draft: true` **nunca genera ruta**, ni en `npm run build` (`dist/blog/como-crear-estrategia-contenidos/` no existe, confirmado) ni en `npm run dev` (`GET /blog/como-crear-estrategia-contenidos/` → `404`, confirmado). Se intentó fijar `draft: false` solo en este fichero local no versionado, únicamente para obtener una ruta renderizada de verificación (nunca con intención de hacer `git add`); el propio entorno de ejecución bloqueó ese `npm run build` por motivos de seguridad (acción de "publicar contenido" sin confirmación), y no se intentó ningún rodeo — la tarea 6.1 revirtió el campo a `draft: true` de inmediato. Esto confirma que la política Q09 (nunca `draft: false` sin confirmación explícita del owner) está bien fundamentada y activa incluso para un fixture no versionado. Verificación adoptada en su lugar: revisión de código de las ramas "con dato" en `[slug].astro`, contrastadas con los valores exactos del fixture — ver las notas añadidas en 3.4, 3.5 y 3.7. No se pudo verificar una renderización end-to-end del fixture completo; los bloques que dependen de datos que sí tienen los 3 posts reales (breadcrumb, taxonomía, metadatos sin actualización, autor, relacionados, CTA por defecto) sí están verificados end-to-end en `dist/blog/sdd-vs-vibe-coding/index.html` (ver 3.1–3.3, 3.6–3.8, 4.1–4.3, 5.1).
- [x] 6.3 Confirmado: el fixture permanece en `draft: true` en el momento de esta verificación (el único intento de `draft: false` fue rechazado por el propio entorno antes de completarse, y se revirtió de inmediato); no se ha pedido ni obtenido confirmación del owner para publicarlo, porque este cambio no publica contenido — solo verifica la página con datos de ejemplo (Q09 de `operations.apply.guidance`).
- [x] 6.4 Eliminado `src/content/blog/como-crear-estrategia-contenidos.mdx`. Verificado con `git status --short` que no aparece como archivo sin trackear ni en staging.

## 7. Regresión y cierre

- [x] 7.1 Ejecutar `npm run build` una última vez sobre el estado final (sin el fixture) y confirmar que los 3 posts reales (`ia-en-ingenieria-de-software`, `perdidos-en-el-medio`, `sdd-vs-vibe-coding`), el listado `/blog` y la portada (`BlogPreview`) siguen compilando y renderizando sin error. Verificado: `62 page(s) built`; `dist/blog/{ia-en-ingenieria-de-software,perdidos-en-el-medio,sdd-vs-vibe-coding}/index.html` y `dist/blog/index.html` existen; `dist/index.html` contiene 3 `blog-card-item` (portada sin cambios).
- [x] 7.2 Revisar manualmente en `npm run dev` los breakpoints 360px, 390px, 768px y 1440px sobre `/blog/sdd-vs-vibe-coding` para confirmar que no hay scroll horizontal y que el ancho de línea del contenido se mantiene legible en desktop (Q02 de `operations.apply.guidance`). Verificado con un script Playwright headless (dev server real, sin modificar código): `overflow: false` y 0 errores de consola en los 4 anchos; `.article-content` mide 360/390px en móvil y se limita a 760px en 768px y 1440px (no full-bleed en desktop). Capturas de pantalla revisadas visualmente en los 4 anchos: breadcrumb, categoría+tags, título, bajada, metadatos, contenido, autor (confirmado visible tras la animación `reveal`), relacionados y CTA aparecen en el orden esperado, sin solapamientos ni cortes. Estados `:focus-visible` confirmados por interacción real de teclado (`.focus()` + lectura de `outlineStyle` computado) en breadcrumb, tag, enlace de autor y botón de CTA: los 4 muestran `outlineStyle: solid`.

## 8. Ajuste posterior: formato de metadatos y posición de tags

Refinamiento pedido tras la primera pasada de `apply`: usar el mismo formato de fecha y tiempo de lectura que `BlogCard` (en vez de fecha larga y "N min de lectura"), alinearlos a la derecha de la categoría en la misma fila (como la fila de cabecera de la tarjeta), y mover los tags a debajo de la bajada en vez de junto a la categoría. Cambia la especificación del "Orden de la estructura" y del "Bloque de metadatos" en `specs/blog/spec.md` y `proposal.md` (delta actualizado in situ, sin nueva propuesta) para reflejar este orden real.

- [x] 8.1 Cambiar el formateo de fecha en `[slug].astro` de largo (`day: 'numeric', month: 'long'`) a corto `dd/mm/aaaa` (mismas opciones que `BlogCard`), y el tiempo de lectura de "N min de lectura" a la insignia con emoji `⏱️ N min` de `BlogCard`; añadir el emoji `📅` delante de la fecha, igual que en `BlogCard`. Verificado con `npm run build` (`62 page(s) built`) y captura Playwright: `📅 08/09/2026` y `⏱️ 5 min` renderizados en `dist`/dev.
- [x] 8.2 Fusionar el bloque de metadatos con la fila de categoría (`article-taxonomy`), con `justify-content: space-between` para que los metadatos queden alineados al extremo opuesto de la categoría, igual que `.blog-card-header-row`. Verificado por captura de pantalla a 1440px: categoría a la izquierda, fecha + tiempo de lectura + (si aplica) actualización a la derecha, misma fila.
- [x] 8.3 Mover el bloque de tags fuera de la fila de categoría, a un bloque propio después de `.article-description`, conservando los mismos enlaces a `/blog?tags=<tag>` y el mismo estilo (`:hover`/`:focus-visible`). Verificado por captura de pantalla: los tags aparecen debajo de la bajada, antes de la imagen destacada/contenido.
- [x] 8.4 Eliminar el CSS ya no usado del bloque `.article-metadata` independiente (sustituido por `.article-meta-group` dentro de `.article-taxonomy`). Verificado con `npm run build` sin errores y sin clases CSS huérfanas referenciadas en el JSX.
- [x] 8.5 Verificar en móvil (390px) que la fila categoría+metadatos envuelve (`flex-wrap`) sin desbordamiento horizontal, y que los tags siguen siendo legibles y clicables debajo de la bajada. Verificado con Playwright: `overflow: false` a 390px; captura de pantalla revisada visualmente.
- [x] 8.6 Actualizar `specs/blog/spec.md` (requisitos "Orden de la estructura de la página de artículo" y "Bloque de metadatos del artículo") y la primera viñeta de "What Changes" en `proposal.md` para que describan el orden real (categoría+metadatos en la misma fila; tags debajo de la bajada) en vez del orden de la primera pasada. `openspec validate` re-ejecutado en verde tras el cambio.
- [x] 8.7 Restilizar `.article-tag` como chip con borde y fondo al hover (mismo patrón que `.filter-chip` de `/blog`), para que los tags se lean claramente como enlaces clicables en vez de texto plano con solo cambio de color. Verificado con `npm run build` (`62 page(s) built`) y capturas Playwright: estado por defecto (borde sutil, sin fondo), `:hover` (borde y fondo `--color-accent-glow`) y `:focus-visible` (`outlineStyle: solid`) confirmados a 1440px; sin overflow horizontal a 390px con los tags envolviendo en dos filas.

## 9. Explorado y revertido: tags al pie de `BlogCard`

Se implementó un bloque de tags identificables al pie de `BlogCard.astro` (listado `/blog`, portada, relacionados), con la restructuración de CSS necesaria para evitar anidar el enlace del tag dentro del enlace principal de la tarjeta (fondo/borde/hover movidos de `.blog-card` a `.blog-card-item`, `outline-offset` del enlace principal pasado a negativo, alturas igualadas por fila con flexbox). Verificado sin errores en las 3 superficies, a 1440px y 390px.

Feedback explícito del usuario tras revisar el resultado: "quedan mal" — se pidió quitar los enlaces de tags de las `BlogCard`. Revertido con `git checkout -- src/components/BlogCard.astro` (el fichero no tenía otras modificaciones pendientes de esta sesión, confirmado con `git log`/`git diff --stat` antes de revertir), devolviéndolo exactamente al estado del commit `80c27cb` (redesign de la tarjeta, ya en `main`). Los requisitos `ADDED`/`MODIFIED` añadidos en `specs/blog/spec.md` para esta funcionalidad se retiraron en el mismo paso, para que la especificación no describa un comportamiento que ya no existe en el código. `BlogCard` queda fuera del alcance final de este change; los tags identificables al pie siguen aplicando solo a la página de artículo (grupo 8).

## 10. Ajuste posterior: el breadcrumb no incluye el título del post

- [x] 10.1 Quitar el título del post de `breadcrumbItems` en `[slug].astro`, dejando el breadcrumb en Inicio / Blog / Categoría. Como los 3 niveles restantes siempre tienen `href`, se simplificó el `.map()` que renderizaba condicionalmente un `<li>` sin enlace (`aria-current="page"`) a un único `<a>` por nivel, y se eliminó el CSS ya no alcanzable de `.breadcrumb li[aria-current='page']` (regla base y su override en el media query de 768px). Verificado con `npm run build` (`62 page(s) built`) e inspección de `dist/blog/sdd-vs-vibe-coding/index.html`: el breadcrumb visible y el JSON-LD `BreadcrumbList` (que se generan de la misma `breadcrumbItems`) terminan los dos en "IA Generativa", sin el título del post. Captura Playwright a 1440px revisada visualmente; foco de teclado en el último nivel del breadcrumb confirmado (`outlineStyle: solid`).
- [x] 10.2 Actualizar el requisito "Breadcrumb contextual en la página de artículo" en `specs/blog/spec.md` para reflejar que el breadcrumb SHALL llegar solo hasta la categoría, sin el título del post. `openspec validate add-blog-post-detail-page --strict` re-ejecutado en verde.
